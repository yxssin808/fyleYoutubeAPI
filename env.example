# YouTube API Environment Variables
# Copy this file to .env.local for local development
# For Vercel deployment, set these in the Vercel dashboard → Settings → Environment Variables

# ============================================
# REQUIRED VARIABLES
# ============================================

# Supabase Configuration
# Get these from your Supabase project → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Frontend URL for CORS
# The URL where your frontend is hosted
FRONTEND_URL=https://fyle-cloud.com

# Google OAuth2 Configuration
# Get these from Google Cloud Console → APIs & Services → Credentials
# Create OAuth 2.0 Client ID with YouTube Data API v3 enabled
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-frontend.com/youtube/oauth/callback

# Storage API URL (for generating signed URLs for S3 files)
# This is the URL of your storage service (e.g., https://fylestorage.vercel.app/api)
# If not set, files with s3_key but no cdn_url will fail
STORAGE_API_URL=https://fylestorage.vercel.app/api

# API Base URL (for video processing - the main API service that has FFmpeg)
# This should point to your main API service (e.g., https://fyle-api.vercel.app)
# The video processing is done by the main API service, not this YouTube API service
API_BASE_URL=https://fyle-api.vercel.app
# Alternative: BACKEND_URL (if you use that name)
# BACKEND_URL=https://fyle-api.vercel.app

# ============================================
# OPTIONAL VARIABLES
# ============================================

# Server Port (default: 4001)
# Only used for local development
PORT=4001

# Node Environment
# Automatically set by Vercel in production
NODE_ENV=production

# Vercel Detection
# Automatically set by Vercel (don't set manually)
# VERCEL=1

